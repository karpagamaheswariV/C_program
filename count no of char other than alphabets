#include <stdio.h>

int main(void) {
  char str[10];
  int count=0;
  printf("Enter a string:");
  scanf("%s",str);
  for(int i=0;str[i]!='\0';i++){
    if(str[i]>=48 && str[i]<=57){
      count++;
    }
  }
  printf("\nThe Numbers in the string : %d",count);
  return 0;
}
